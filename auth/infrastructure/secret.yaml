apiVersion: v1
kind: Secret
metadata:
  name: authentik-secrets
  namespace: default
type: Opaque
stringData:
  AUTHENTIK_SECRET_KEY: ""
  # These are placeholders referencing Secret Manager.
  # If you are not using a sync operator, replace these with the actual credentials.
  AUTHENTIK_POSTGRESQL__USER: ""
  AUTHENTIK_POSTGRESQL__PASSWORD: ""
  AUTHENTIK_BOOTSTRAP_PASSWORD: ""
